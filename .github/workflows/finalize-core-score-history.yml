name: Finalize official EOD Core Score history

on:
  schedule:
    # Limited Taipei evening retries. Holidays and not-yet-published sources
    # remain SOURCE_NOT_READY and never publish or backfill a snapshot.
    # 23:30 is the final bounded retry; on 2026-08-25 the official fallback
    # had matured after the 22:30 retry while the primary was still stale.
    - cron: "30 7,12,14,15 * * 1-5"
    - cron: "0 10 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: hs-eod-core-history
  cancel-in-progress: false

jobs:
  finalize:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Configure EOD history committer
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main
      - name: Fetch validated official EOD market input
        run: |
          expected_date="$(TZ=Asia/Taipei date +%F)"
          python scripts/fetch_official_eod_market.py \
            --expected-date "$expected_date" \
            --output "${RUNNER_TEMP}/official-eod-market.json"
      - name: Finalize expected Taipei trading date only
        run: |
          expected_date="$(TZ=Asia/Taipei date +%F)"
          node scripts/finalize_core_score_history.js \
            --eod "${RUNNER_TEMP}/official-eod-market.json" \
            --expected-date "$expected_date"
      - name: Commit finalized EOD history only
        run: |
          git add finalized-core-score-snapshots-v1.json
          git diff --cached --quiet || git commit -m "Update official ETF radar score history"
          git pull --rebase
          git push
